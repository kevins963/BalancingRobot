#ifndef ROBOT_CORE_INTEGER_H_
#define ROBOT_CORE_INTEGER_H_

#include <stdbool.h>
#include <stdint.h>

#define MIN(a, b) ((a < b) ? a : b)
#define MAX(a, b) ((a > b) ? a : b)

#endif  // ROBOT_CORE_INTEGER_H_
